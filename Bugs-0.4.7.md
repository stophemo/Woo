# 0.4.7 Bug & 优化跟踪

> 用于记录 v0.4.7 阶段发现的 Bug 和待优化项，待 v0.4.8 发布时逐一验证是否已修复。

---

## UI 响应性

### [x] AI 文件夹操作 UI 不即时刷新
- **描述**：AI Agent 创建/重命名/删除目录后，左侧目录树没有及时响应变化，缺少缓入/缓出动画。
- **解决时间**：2026-06-01
- **验证人**：
- **0.4.8 验证结果**：

### [x] AI 文稿操作 UI 不即时刷新
- **描述**：AI Agent 创建/删除文稿后，缩略图列和文稿列表没有及时响应变化，缺少缓入/缓出动画。
- **解决时间**：2026-06-01
- **验证人**：
- **0.4.8 验证结果**：

---

## 加锁/解锁

### [x] 文档加锁后右键菜单仍显示"加锁"
- **描述**：右键缩略图 → 加锁 → 成功后再次右键，菜单仍显示"加锁"而非"解锁"。原因是 `syncRefresh()` 的文档 merge 循环未同步 `isLocked` 字段。
- **解决时间**：2026-06-01
- **验证人**：
- **0.4.8 验证结果**：

## 待添加

（请在下方按 `[ ] 标题` 格式添加新的条目，填写描述和期望行为）

---

## 拖拽排序

### [x] 目录树拖拽排序
- **描述**：支持鼠标拖拽目录项以切换同级目录的展示顺序。hover 时右侧显示拖拽手柄（六点网格图标），拖至目标位置上方/下方显示蓝色指示线，松手后自动持久化排序。
- **解决时间**：2026-06-01
- **验证人**：
- **0.4.8 验证结果**：

### [x] 文稿略缩图拖拽排序
- **描述**：支持鼠标拖拽略缩图以切换当前目录下文稿的展示顺序。hover 时右上角显示拖拽手柄（六点网格图标），拖至目标上方/下方显示蓝色指示线，松手后自动持久化排序。
- **解决时间**：2026-06-01
- **验证人**：
- **0.4.8 验证结果**：

