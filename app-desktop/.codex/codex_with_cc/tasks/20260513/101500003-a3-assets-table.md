任务名: asset-link-and-excel-table
工作目录: D:\coding\Woo\app-local
目标:
1) 支持配置第三方网盘/对象存储作为图片等静态资源链接前缀（至少可配置: provider名、baseUrl、可选pathPrefix），并在插入图片时可自动拼接。
2) 中央编辑区域内表格支持基础 Excel 能力：
   - 单元格文字格式（至少加粗/斜体/对齐）
   - 顶部表头、左侧表头（左侧表头可通过首列样式或开关实现）
   - 自定义函数计算（至少支持 =SUM(A1:A3)、=AVG(A1:A3)、=MIN、=MAX 以及简单 A1+B1）
3) 提供最小可用 UI（工具栏按钮或右键菜单均可），并保证文档内容持久化。
约束:
- 尽量限制在编辑器扩展、样式、设置和相关工具文件。
- 代码需通过 npm run build。
输出:
- 修改文件清单
- 验证命令与结果
- 固定标题报告。
